# External dependencies (installed packages using npm)
node_modules/

# Production build output (generated when building the app for deployment)
build/

# Environment configuration file (stores API keys and secret variables)
.env

# Local backup files (old "copy" files - not needed in version control)
.backups/

# Migration script (temporary - not needed in repo)
migrate.ps1
